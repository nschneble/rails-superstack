<% if current_user %>
  <div class="px-4 py-2 text-xs text-slate-600 select-none">
    <span>Signed in as</span>
    <span class="font-medium text-slate-700 break-all"><%= current_user.email %></span>
  </div>

  <div class="border-t border-slate-300"></div>
<% end %>
