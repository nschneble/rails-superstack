<%# Empty-state message for the API tokens list. Pass `hidden: true` when
    tokens exist (Turbo updates toggle it) %>
<%# locals: (hidden:) %>

<%= tag.p class: "italic text-xs", id: "api_tokens_empty", hidden: hidden do %>
  <%= t("settings.api_tokens.empty.message") %>
<% end %>
