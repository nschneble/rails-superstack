---
detectors:
  # Normalizers are stateless transformation objects.
  # Reek flags the `call` method as UtilityFunction because it doesn't use any
  # instance variables, but normalizers are intentionally stateless.
  UtilityFunction:
    enabled: false
